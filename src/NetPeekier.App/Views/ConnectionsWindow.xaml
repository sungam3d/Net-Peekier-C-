<Window x:Class="NetPeekier.App.Views.ConnectionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Connections"
        Width="780" Height="500" MinWidth="600" MinHeight="320">

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#eef1f4"
                BorderBrush="#d0d5da" BorderThickness="0,0,0,1" Padding="10,8">
            <StackPanel>
                <TextBlock x:Name="Header" FontWeight="Bold" FontSize="13" />
                <Grid Margin="0,6,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Path: "        Foreground="#555" Margin="0,0,6,2" />
                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="ExePath"     TextTrimming="CharacterEllipsis" FontFamily="Consolas" />
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Description: " Foreground="#555" Margin="0,0,6,2" />
                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="ExeDesc" />
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Company: "     Foreground="#555" Margin="0,0,6,2" />
                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="ExeCompany" />
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Version / size: " Foreground="#555" Margin="0,0,6,0" />
                    <TextBlock Grid.Row="3" Grid.Column="1" x:Name="ExeVersion" />
                </Grid>
            </StackPanel>
        </Border>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem><TextBlock x:Name="StatusLine" Text="" /></StatusBarItem>
        </StatusBar>

        <DataGrid x:Name="ConnGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  AlternationCount="2"
                  RowBackground="White"
                  AlternatingRowBackground="#F5F7FA"
                  MouseDoubleClick="OnConnectionDoubleClick">
            <DataGrid.ToolTip>
                <TextBlock Text="Double-click a connection to open the packet feed for this process." />
            </DataGrid.ToolTip>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Proto"      Binding="{Binding Protocol}"        Width="60"  />
                <DataGridTextColumn Header="Dir"        Binding="{Binding DirectionArrow}"  Width="50"  />
                <DataGridTextColumn Header="Local IP"   Binding="{Binding LocalIp}"         Width="130" />
                <DataGridTextColumn Header="Lport"      Binding="{Binding LocalPort}"       Width="60"  />
                <DataGridTextColumn Header="Remote IP"  Binding="{Binding RemoteIp}"        Width="160" />
                <DataGridTextColumn Header="Rport"      Binding="{Binding RemotePort}"      Width="60"  />
                <DataGridTextColumn Header="Status"     Binding="{Binding Status}"          Width="100" />
                <DataGridTextColumn Header="↑ Bps"      Binding="{Binding UpBps}"           Width="80"  />
                <DataGridTextColumn Header="↓ Bps"      Binding="{Binding DownBps}"         Width="80"  />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
